# All optional. The site deploys and renders fully static without any of these.

# Where /api/forms delivers submissions (Slack/Teams/Zapier/CRM incoming webhook).
# Unset = submissions are logged to the function console instead of sent.
FORM_WEBHOOK_URL=
